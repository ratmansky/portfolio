# Local Content Overrides

Place local-only translation or draft content files here.

Suggested filename:

- `ru.local.js`

These files are ignored by git via `.gitignore`, so they stay available on localhost only and never ship to production by default.
